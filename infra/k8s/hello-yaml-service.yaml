apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-yaml
  name: hello-yaml
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: hello-yaml
  type: ClusterIP
status:
  loadBalancer: {}
